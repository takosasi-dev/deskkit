# Changelog

## [0.3.0] - 2026-09-25

### Added
- SendPrep(共有前クリーナー): 写真・スクリーンショット・動画を放り込むだけで、位置情報などの隠れた情報を消し、送り先のサイズ上限に収め、
  見せたくない文字を塗りつぶした別のファイルを作る。元のファイルは変えない。エクスプローラーの「送る」から渡すこともできる。
- PcCheckup(PC 不調の診断): 「重い」「ネットが遅い」「容量が足りない」のボタンで原因の候補を調べ、次にやることを示す。設定は変えず、案内するだけ。
- TwinSweep(似た写真の整理): フォルダの中のそっくりな写真をグループにまとめ、いちばん良い1枚を提案する。残りは確認してからごみ箱へ送る。
- 3つとも初期状態は無効で、利用者が操作したときだけ動く(自動で動くのは PcCheckup の「空き容量の見張り」だけで、既定はオフ)。通信はしない。
- 本体: ファイルをごみ箱へ送る共通部品(ごみ箱の無いドライブのファイルは送らずに残す。恒久削除にならない)。
- 本体: 設定画面の「ライセンス」。同梱しているライブラリと ffmpeg のライセンス全文とソースの入手先(THIRD_PARTY_LICENSES.txt)を表示する。
- 本体: `DeskKit <モジュール> open <ファイル...>` で、DeskKit が起動していなければ起動してから渡す(最大 15 秒待つ)。1回で 200 個・合計 32,000 文字までのパスを渡せる。
  受け取るモジュールが無効なときは、その旨を通知する。
- 本体: 3モジュールの色(ライト用のアクセント色とグラフの色。7 色で明度・色覚差・コントラストを両テーマで検証)。

### Changed
- DeskKit.exe が大きくなる(v0.2.0 の約 40MB から、100MB 前後の見込み。実測は docs/v0.3/measurements.md)。
  写真の読み込み(Pillow・pi-heif)・計算(NumPy)・PC の情報(psutil)・Windows の文字認識(PyWinRT)の各ライブラリと、
  動画の処理に使う ffmpeg(LGPL 版。LZMA で圧縮した zip のまま入れ、初めて動画を扱うときだけ展開する)を同梱するため。
  ライブラリは使うときに初めて読み込むので、起動の速さと常駐のメモリはほとんど変わらない。
- 本体: テスト・自己検査用にデータ置き場(`DESKKIT_HOME`)を分けた DeskKit は、本番の常駐とは別のインスタンスとして動く。
- 本体: 再起動・更新で DeskKit.exe を起動し直すとき、新しいプロセスが古いプロセスの展開先フォルダを使わないようにした。
- 本体: ログ・停止の理由・診断レポートに残す例外は、型名と DeskKit のソースの位置だけにした(例外の文に入りうるファイル名・パスを書かない)。
- 本体: NumPy の計算スレッドを 1 にして起動する(何もしなくても約 490MB 確保されるのを避ける。環境変数 `OPENBLAS_NUM_THREADS` で変えられる)。

## [0.2.0] - 2026-09-25

### Added
- 本体: 一時停止(トレイ・ホーム・クイックアクションから 30分 / 1時間 / 再開するまで。自動で動く処理だけを止める)。
- 本体: ゲーム・全画面中は通知を保留し、終わってから「保留中の通知 N 件」にまとめて表示(エラーはすぐ表示)。
- 本体: 設定の自動世代保存(直近 20 世代)と、設定画面からの巻き戻し。
- 本体: 診断レポートのコピー(パス・ユーザー名・本文・exe 名を含まない)。
- 本体: トレイの「クイックアクション」、ホームのモジュールカード全体をクリックで開く、「最近の通知」をクリックで開く。
- ModeShift: 電源(AC / バッテリー)の切り替わりをきっかけにした自動切替。
- ModeShift: 新しいアクション「マイクのミュート・録音音量」「アプリのダーク/ライト」(どちらも元に戻せる)。
- ModeShift: 元に戻したときのイベント `modeshift.reverted` と、再起動時に今のモードを知らせる `modeshift.switched`(restored)。
- DropSort: ルールを試す(ファイル名などを入れると、当たるルールと行き先を表示。ファイルには触らない)。
- DropSort: 移動先テンプレート `{yyyy}` `{mm}` `{dd}` `{ext}` `{domain}`(基準フォルダの下だけ自動で作る)。
- DropSort: ルールごとの直近 14 日の実績と、条件を満たした試運転ルールへの「本番へ切り替え」の提案。
- DropSort / ClipShelf: ModeShift の指定モード中は自動の整理・記録を止める。
- LayoutKeep: 1つのモニタ構成に名前付きプリセットを複数持てる(ModeShift の `layout_apply` からも名前で呼べる)。
- LayoutKeep: 自動スナップショットと「抜く前の配置に戻す」。
- LayoutKeep: 新しく開いたウィンドウを保存した位置へ置く(既定オフ・試運転から)。
- ClipShelf: 定型文の入力欄・選択欄 `{input:ラベル}` `{select:A|B|C}`。
- ClipShelf: 変換して貼り付け(Shift+Enter。空白除去・改行除去・全角英数を半角・大文字・小文字・1行目だけ)。
- ClipShelf: 指定アプリからのコピーを一定時間で自動的に消す。

### Fixed
- ログ画面をログ出力スレッドから直接書き換えていて、落ちることがあった。
- 更新の入れ替え・巻き戻しに失敗したとき、DeskKit.exe が無くなることがあった。
- 更新・再起動の直後に、旧プロセスの終了が遅いと常駐が消えることがあった。
- 設定の host 値が不正(null・文字列)だと起動のたびに落ちていた。
- 更新の通信で、リダイレクト先を許可リストで確かめる前に接続していた。
- ダウンロードした更新ファイルが消されずに溜まっていた。
- ModeShift: 強制終了の確認を待つ間に PID が再利用されると、別のプロセスを終了させうる。
- DropSort: コマンドラインからの整理中に画面・トレイ・ホットキーが固まっていた。
- LayoutKeep: タスクバーを左や上に置いた副モニタで、ウィンドウの位置がずれて保存されていた。
- ClipShelf: 保持件数・日数をホイールで変えただけで履歴が即削除されていた(確認を出すようにした)。
- ClipShelf: 全消去で、復号できない履歴の行が残っていた。
- 数値欄・選択欄がページのスクロール中にホイールで変わっていた。
- exe のプロパティの説明が文字化けしていた。

### Changed
- ModeShift の操作記録(ops.jsonl)には、開いた URL のドメインだけを残す(パス・クエリは書かない)。
- 利用状況の集計を別スレッドで行い、期間と表示の選択を覚えるようにした。

## [0.1.0] - 2026-09-24

### Added
- host(常駐本体): 単一インスタンス、トレイ、設定(settings.json の原子的書き込み・セクション単位の検証)、ログ(日次ローテーション・faulthandler)、
  隠しウィンドウによるシステムメッセージ配布、ホットキー(OverlayKit 互換 HotkeyRegistry を同梱)、前面ウィンドウ情報(ゲーム・全画面・昇格の判定)、
  イベントバス、CLI 転送(名前付きパイプ)、自動起動(HKCU Run)、モジュールの隔離(例外で1つだけ停止)、`--selftest`。
- Control Center(GUI): ホーム(モジュールカード・前面状態・通知履歴・ホットキー一覧)、各モジュール画面、共通設定、ログ表示。通知トースト。
- モジュール: ModeShift / DropSort / LayoutKeep / ClipShelf(初期状態はすべて無効。有効化後は試運転から始まる)。
- 単体 exe(PyInstaller onefile)のビルドスクリプト `build.ps1`(DeskKit.exe.sha256 も出力)。
- クイックアクション(Ctrl+Alt+Space の検索窓。トレイ項目と各モジュールの追加操作を名前で実行)。
- 利用状況ページ(モジュールごとの日別件数の棒グラフ/表、7/30/90日)。
- GitHub Releases からの自動更新(確認→通知→ワンクリック更新、SHA-256 検証、試し起動、前の版に戻す)。
- 設定のバックアップ/復元、はじめてガイド、ライトテーマと Windows 連動。
